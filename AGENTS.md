输出语言：简体中文。

尽可能少使用行间 LaTeX 公式，除非公式较长或地位极其核心。

## Agent skills

### Issue tracker

任务与规格使用 GitHub Issues 管理；操作前读取 `docs/agents/issue-tracker.md`。

### Triage labels

采用默认的五种分诊标签；分诊前读取 `docs/agents/triage-labels.md`。

### Domain docs

采用 single-context 布局；探索代码库前读取 `docs/agents/domain.md`。
