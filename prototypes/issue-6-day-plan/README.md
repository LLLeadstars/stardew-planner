# 丢弃原型 · 星露谷日程管理器 V1 交互原型

> **这不是产品代码，不要合并进 main。**
> 本分支（`prototype/issue-6-day-plan`）保存原型的完整快照，作为决策的 primary source。
> 代码按「只求能跑」的标准写：无错误处理、无抽象、无测试覆盖承诺，**不得直接提升为产品实现**。

## 它回答什么问题

来源票：[#6 验证电脑端规划主流程与编辑交互](https://github.com/LLLeadstars/stardew-planner/issues/6)

> 玩家如何从首次进入到排好一天，完成选活动、填参数、查看估算、排序、固定时间、处理冲突和关联目标？
> 通过可丢弃的电脑端原型验证**信息布局与编辑方式**，同时覆盖首次使用、空状态、冲突和恢复已保存数据。

## 怎么跑

**双击 `index.prototype.html`** 即可。自包含单文件：无构建、无依赖、无网络请求。

自检（需要 Node，任意版本）：

```
node selftest.model.js     # 模型层断言
node selftest.render.js    # 渲染烟雾扫描 + 结构断言
```

## 内容

| 文件 | 说明 |
|---|---|
| `index.prototype.html` | 原型本体。含两个变体（底部切换条或 ← → 键切换，共享同一份数据）：<br>**D** 融合 · 按时间定位的单列表（时间轴 × 列表合为一列）<br>**F** 三栏 · 左信息栏 + 日程 + 检查器 |
| `variants-archive.prototype.html` | 变体快照，含已删除的 A 时间轴 / B 列表+检查器 / C 托盘+时间槽 / E 线性轴。`prototype` 技能的 UI 分支要求「完整变体集作为 primary source 保留」，删除的变体也在这份里 |
| `selftest.model.js` | 模型层断言 |
| `selftest.render.js` | 渲染烟雾扫描与结构断言 |

本快照的规模：模型断言 **105 条**、结构断言 **43 条**、渲染扫描 **22 次**，全部通过。

## 当前已定的交互决定

完整决策记录发在 [#6](https://github.com/LLLeadstars/stardew-planner/issues/6) 的结论评论里（不随本分支分发）。要点：

- 日程形态：**融合**——时间轴与列表合为一个按时间定位的单列表，不做两栏并排
- 重叠表达：包含关系用**缩进**；部分重叠用**脊柱 + 「同时进行 · 重叠 Xm」**
- 拖动语义：压缩轴上**不做连续拖动**，改用**离散落点**（空隙起点 / 卡片上半＝紧贴到它前面 / 卡片下半＝紧贴到它后面）；相邻两项互换开始时刻用 ↑↓
- 空隙留白：**紧凑**（压到 30px）与**真实比例**（不封顶）两档，**默认真实比例**

## 关于自检脚本（有意偏离技能）

`prototype` 技能的反模式清单第一条写着「**Don't add tests.** A prototype that needs tests is no longer a prototype.」

这里保留自检是**有意的偏离**：该原型的模型层承载大量日期算术与领域规则（10 分钟粒度、工具升级 D+2、包含关系、时长来源优先级链、门店条件判定）。这些规则若**静默算错**，玩家就是在对着一个错误的物件做判断，反馈会污染决策——比不做原型更糟。

所以这两份脚本守的不是产品质量，而是**判断对象的有效性**。代价是它们占约 29KB，且模型改动时须同步维护。

## 已知的占位与缺口

- **浇水估算系数是占位值**（每格 0.5 分、每次往返补水 5 分、密集 ×1.25 / 分散 ×1.5、校准上界 120 格）。真实数字待 `执行浇水耗时实测与校准`（[#11](https://github.com/LLLeadstars/stardew-planner/issues/11)）。界面内可展开查看。
- **皮埃尔杂货店的数据标着「待核验」**，完整门店矩阵待 `核验首批门店营业、交易与条件性例外矩阵`（[#12](https://github.com/LLLeadstars/stardew-planner/issues/12)）。
- **作物批次只做最薄一层**：按已完成种植活动派生，按数据字段算首次收获日期，不做逐日生长模拟。
- **重复活动只是一个次数输入框**，不生成实例、不做重算。
- **所有活动不分日期**：切换「查看日程」日期不会过滤活动列表（跨天规划在 V1 范围外）。
- **拖动的手感未优化**：落点语义已定，具体的跟手、吸附、幽灵元素等留给实现阶段。
